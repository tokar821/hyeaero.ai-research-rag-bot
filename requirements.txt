# HyeAero Backend - RAG Pipeline + API

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Vector Database
pinecone>=3.0.0

# Embeddings
openai>=1.0.0

# RAG semantic reranker (BAAI bge-reranker-large); optional if RAG_RERANK_ENABLED=0
sentence-transformers>=2.6.0

# Database
psycopg2-binary>=2.9.0

# ZoomInfo (owner enrichment for PhlyData)
requests>=2.28.0

# Optional: FAA trustee web hints (PhlyData owners). REST works with requests alone; SDK optional.
tavily-python>=0.5.0

# Optional: RAG answer cache for Ask Consultant (set REDIS_URL).
redis>=5.0.0

# Environment & Config
python-dotenv>=1.0.0

# Utilities
pydantic>=2.0.0
typing-extensions>=4.8.0

# Logging (reuse from ETL pipeline)
# Note: If running independently, may need to install:
# - logging utilities
