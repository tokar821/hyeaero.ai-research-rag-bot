# Static data files

PhlyData owner **operating-company hints** for trustee-style FAA registrants use **Tavily** (web search) plus **OpenAI** synthesis — not files in this folder.

See `backend/README.md` (`TAVILY_API_KEY`, `OPENAI_API_KEY`) and `services/tavily_owner_hint.py` / `services/tavily_llm_synthesis.py`.
